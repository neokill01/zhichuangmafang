.DS_Store
node_modules/
*.log
.workbuddy/

# 淘宝相关素材（本地物料，不入库）
taobao*
推广文案.md

# 运维配置（nginx vhost 等）：不含机密，但属侦察素材（活着的站点 + 技术栈 + 目录结构）
# 只留服务器/本地，不入库。*.conf 一并忽略，避免换个文件名又混进来。
.deploy/
*.conf

# deploy.yml 在 runner 仓库根创建的暂存目录（本地复现部署步骤时会留下）
.deploy-stage/
