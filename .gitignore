.DS_Store
node_modules/
*.log
.workbuddy/

# 淘宝相关素材（本地物料，不入库）
taobao*
推广文案.md

# 运维配置（nginx vhost 等）：不含机密，但属侦察素材（活着的站点 + 技术栈 + 目录结构）
# 只留服务器/本地，不入库。所有 .conf 一律忽略（含 .conf.* 这类模板/备份命名）；
# 放行示例只会开口子——实测 !*example*.conf 会让名为 nginx-example.conf 的真实 vhost
# 变成可入库。要写示例请写进 README 或 .md。
# 无法穷举所有命名（nginx/default、site.nginx 挡不住），根本办法是别把运维配置放进工作区。
.deploy/
*.conf
*.conf.*
*.conf~

# 凭据类：这才是真正要保密的东西，绝不能进这个 public 仓库
.env
.env.*
!.env.example
!.env.sample
!.env.template
.envrc
*.pem
*.key
*.p12
*.ppk
*.jks
*.p8
*.pem.*
*.key.*
id_ecdsa*
id_dsa*
*_rsa*
*_ed25519*
.netrc
.npmrc
credentials.json
service-account*.json
kubeconfig
*.tfstate

# deploy.yml 在 runner 仓库根创建的暂存目录（本地复现部署步骤时会留下）
.deploy-stage/
